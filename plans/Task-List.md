# Improvements, Requirements, Requests

- Set up distinct tmux configurations for local mac and remote GPU servers
    - tmux conf on remotes must use distinct prefix (leader key) than local (e.g. local: ctrl + b, remote: ctrl + a)
